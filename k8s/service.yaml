apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  labels:
    app: notifications-api
spec:
  type: ClusterIP
  selector:
    app: notifications-api
  ports:
    - name: http
      protocol: TCP
      port: 5058
      targetPort: 5058
